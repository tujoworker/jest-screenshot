import { JestScreenshotConfiguration } from "./config";
/**
 * This function is used to setup and initialize **jest-screenshot**.
 *
 * A configuration object can be passed as the first argument.
 */
export declare function setupJestScreenshot(customConfig?: JestScreenshotConfiguration): void;
